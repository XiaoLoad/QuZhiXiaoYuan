<?xml version="1.0" encoding="utf-8"?>
<!--
  2x2 竖向小组件（浅色）
  第一行：emoji + 设备名（卫生间 / 洗手台）
  第二行：「上次使用的设备」
  其余空间：圆形开关水按钮，居中偏下

  按钮放两个 TextView 切 visibility，而不是一个换背景：
  RemoteViews 换背景只能靠 setInt(id,"setBackgroundResource",...) 反射调用，
  框架对反射方法有 @RemotableViewMethod 白名单，赌错会让整个小组件渲染失败。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg_light"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/widget_emoji"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:text="🚿" />

        <TextView
            android:id="@+id/widget_device"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:textSize="13sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="#111827"
            android:text="未绑定设备" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_sublabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textSize="10sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#6B7280"
        android:text="上次使用的设备" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <TextView
            android:id="@+id/widget_action_start"
            android:layout_width="58dp"
            android:layout_height="58dp"
            android:layout_gravity="center_horizontal|bottom"
            android:layout_marginBottom="2dp"
            android:background="@drawable/widget_circle_start_light"
            android:gravity="center"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:text="开" />

        <TextView
            android:id="@+id/widget_action_stop"
            android:layout_width="58dp"
            android:layout_height="58dp"
            android:layout_gravity="center_horizontal|bottom"
            android:layout_marginBottom="2dp"
            android:background="@drawable/widget_circle_stop_light"
            android:gravity="center"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:text="关"
            android:visibility="gone" />
    </FrameLayout>
</LinearLayout>
